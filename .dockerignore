# ==============================================================================
# aws-iam-monitor dockerignore
#
# The Docker image contains the Bash implementation only (project/bash/) —
# see Dockerfile. Everything else is excluded from the build context.
# ==============================================================================

project/bash/.env
